export interface toDoListType {
  id?: number
  done: boolean
  text?: string
  time: string

  // [proName: string]: an
}
export interface toDoListItemType {
  id?: number
  done: boolean
  text?: string
  time: string
}
